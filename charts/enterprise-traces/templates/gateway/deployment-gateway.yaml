apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "enterprise-traces.gatewayFullname" . }}
  labels:
    {{- include "enterprise-traces.gatewayLabels" . | nindent 4 }}
    {{- with .Values.gateway.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- with .Values.gateway.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  replicas: {{ .Values.gateway.replicas }}
  selector:
    matchLabels:
      {{- include "enterprise-traces.gatewaySelectorLabels" . | nindent 6 }}
  strategy:
    {{- toYaml .Values.gateway.strategy | nindent 4 }}
  template:
    metadata:
      labels:
        {{- include "enterprise-traces.gatewaySelectorLabels" . | nindent 8 }}
        {{- with .Values.gateway.labels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        {{- if .Values.useExternalConfig }}
        checksum/config: {{ .Values.externalConfigVersion }}
        {{- else }}
        checksum/config: {{ include (print $.Template.BasePath "/secret-config.yaml") . | sha256sum }}
        {{- end}}
        {{- with .Values.gateway.annotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ template "tempo.serviceAccountName" . }}
      {{- if .Values.gateway.priorityClassName }}
      priorityClassName: {{ .Values.gateway.priorityClassName }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.gateway.securityContext | nindent 8 }}
      initContainers:
        {{- toYaml .Values.gateway.initContainers | nindent 8 }}
      {{- if .Values.image.pullSecrets }}
      imagePullSecrets:
      {{- range .Values.image.pullSecrets }}
        - name: {{ . }}
      {{- end }}
      {{- end }}
      containers:
        - name: enterprise-traces
          image: "{{ template "enterprise-traces.image" . }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - -target=gateway
            - -config.file=/etc/tempo/config/config.yaml
            {{- if .Values.minio.enabled }}
            - -admin.client.backend-type=s3
            - -admin.client.s3.endpoint={{ template "enterprise-traces.minio" . }}
            - -admin.client.s3.bucket-name=enterprise-traces-admin
            - -admin.client.s3.access-key-id={{ .Values.minio.accessKey }}
            - -admin.client.s3.secret-access-key={{ .Values.minio.secretKey }}
            - -admin.client.s3.insecure=true
            {{- end }}
            {{- if .Values.gateway.useDefaultProxyURLs }}
            - -gateway.proxy.default.url=http://{{ template "enterprise-traces.fullname" . }}-admin-api.{{ .Release.Namespace }}.svc:{{ .Values.server.http_listen_port }}
            - -gateway.proxy.admin-api.url=http://{{ template "enterprise-traces.fullname" . }}-admin-api.{{ .Release.Namespace }}.svc:{{ .Values.server.http_listen_port }}
            - -gateway.proxy.distributor.url=http://{{ template "enterprise-traces.fullname" . }}-distributor.{{ .Release.Namespace }}.svc:{{ .Values.server.http_listen_port }}
            - -gateway.proxy.ingester.url=http://{{ template "enterprise-traces.fullname" . }}-ingester.{{ .Release.Namespace }}.svc:{{ .Values.server.http_listen_port }}
            - -gateway.proxy.query-frontend.url=http://{{ template "enterprise-traces.fullname" . }}-query-frontend.{{ .Release.Namespace }}.svc:{{ .Values.server.http_listen_port }}
            - -gateway.proxy.ruler.url=http://{{ template "enterprise-traces.fullname" . }}-ruler.{{ .Release.Namespace }}.svc:{{ .Values.server.http_listen_port }}
            {{- end }}
            {{- range $key, $value := .Values.gateway.extraArgs }}
            - "-{{ $key }}={{ $value }}"
            {{- end }}
          volumeMounts:
            - name: config
              mountPath: /etc/tempo/config
            - name: license
              mountPath: /etc/enterprise-traces/license
            - name: storage
              mountPath: /data
              subPath: {{ .Values.gateway.persistence.subPath }}
            {{- if .Values.gateway.extraVolumeMounts }}
            {{ toYaml .Values.gateway.extraVolumeMounts | nindent 12 }}
            {{- end }}
          ports:
            - name: http-metrics
              containerPort: {{ .Values.server.http_listen_port }}
              protocol: TCP
          livenessProbe:
            {{- toYaml .Values.gateway.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.gateway.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.gateway.resources | nindent 12 }}
          securityContext:
            readOnlyRootFilesystem: true
          env:
            {{- if .Values.gateway.env }}
            {{ toYaml .Values.gateway.env | nindent 12 }}
            {{- end }}
        {{- with .Values.gateway.extraContainers }}
        {{ toYaml . | nindent 8 }}
        {{- end }}
      nodeSelector:
        {{- toYaml .Values.gateway.nodeSelector | nindent 8 }}
      affinity:
        {{- toYaml .Values.gateway.affinity | nindent 8 }}
      tolerations:
        {{- toYaml .Values.gateway.tolerations | nindent 8 }}
      terminationGracePeriodSeconds: {{ .Values.gateway.terminationGracePeriodSeconds }}
      volumes:
        - name: config
          secret:
          {{- if .Values.useExternalConfig }}
            secretName: {{ .Values.externalConfigName }}
          {{- else }}
            secretName: enterprise-traces-config
          {{- end }}
        - name: license
          secret:
          {{- if .Values.useExternalLicense }}
            secretName: {{ .Values.externalLicenseName }}
          {{- else }}
            secretName: enterprise-traces-license
          {{- end }}
        - name: storage
          emptyDir: {}
        {{- if .Values.gateway.extraVolumes }}
        {{ toYaml .Values.gateway.extraVolumes | nindent 8 }}
        {{- end }}
